<div class="container">
  <div class="row">
    <div class="col-10">
      <h2>ログイン</h2>
      <p>会員の方はこちらから</p>
      <div class="text-center">
        <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
          
          <div class="form-group col-10">
            <%= f.label :"メールアドレス", class: "col-4" %>
            <%= f.email_field :email, autofocus: true,autocomplete: "email", class: "col-lg-5" %>
          </div>
          
          <div class="form-group col-10">
            <%= f.label :"パスワード", class: "col-4" %>
            <%= f.password_field :password, autofocus: true,autocomplete: "current-password", class: "col-lg-5" %>
          </div>
        
          <% if devise_mapping.rememberable? %>
            <div class="field">
              <%= f.check_box :remember_me %>
              <%= f.label :remember_me %>
            </div>
          <% end %>
        
          <div class="actions">
            <div class=" col-10">
            <%= f.submit "ログイン", class: "btn btn-primary text-left" %>
            </div>
          </div>
        <% end %>
      </div>
    </div>

    <div class="mt-5">
      登録がお済みではない方は<%= link_to "こちら", new_registration_path(resource_name) %>から新規登録を行ってください。
    </div>
    </div>
  </div>
</div>



